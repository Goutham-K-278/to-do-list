{
  "name": "mini-todo",
  "version": "1.0.0",
  "description": "MERN stack Mini Todo App — run both frontend and backend with a single command",
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"magenta,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "server": "npm run backend",
    "client": "npm run frontend",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "lint": "npm run lint --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.8.2"
  }
}
